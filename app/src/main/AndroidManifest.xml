<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.tvz.hr.happyenvelope">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name="androidx.tvz.hr.happyenvelope.ui.gifts.view.GiftDetail"
            android:label="@string/title_activity_gift_detail"
            android:theme="@style/Theme.MyAppTheme.NoActionBar"></activity>
        <activity
            android:name="androidx.tvz.hr.happyenvelope.MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>